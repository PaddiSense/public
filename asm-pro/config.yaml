# GENERATED from the source config.yaml by release.sh Step 7 (WR-PS-115).
# DO NOT hand-edit — any edit is overwritten at the next release.
# source: PaddiSense/ASM-Pro@e4786329af8c69ef55acfc9ce3da02916a917f94
# Catalog-only deltas are declared in contracts/catalog-overrides.yaml;
# contracts/check-catalog-drift.py verifies catalog == source on every release + in CI.
name: PaddiSense Assets
version: 2026.8.7
slug: asm-pro
description: PaddiSense Assets — asset management, maintenance tracking, and parts
  inventory
logo: true
icon: true
url: https://github.com/PaddiSense/ASM-Pro
arch:
- amd64
- aarch64
startup: application
init: false
ingress: true
ingress_port: 8102
panel_icon: mdi:wrench
panel_title: Assets
homeassistant_api: true
hassio_api: true
hassio_role: manager
map:
- addon_config:rw
- media:rw
- share:rw
options:
  db_host: 77b2833f-timescaledb
  db_port: 5432
  db_name: asmpro
  db_user: postgres
  db_password: homeassistant
  admin_key: ''
  seed_author_mode: false
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  admin_key: password?
  seed_author_mode: bool?
image: ghcr.io/paddisense/{arch}-asm-pro
