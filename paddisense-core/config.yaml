# GENERATED from the source config.yaml by release.sh Step 7 (WR-PS-115).
# DO NOT hand-edit — any edit is overwritten at the next release.
# source: PaddiSense/Core@c4a213a201775df967deb6412e68d0ad359a15a9
# Catalog-only deltas are declared in contracts/catalog-overrides.yaml;
# contracts/check-catalog-drift.py verifies catalog == source on every release + in CI.
name: PaddiSense Core
version: 2026.7.33
slug: paddisense-core
description: PaddiSense Core — system gateway, licence management, addon health monitoring
logo: true
icon: true
url: https://github.com/PaddiSense/PaddiSense-Core
arch:
- amd64
- aarch64
startup: application
init: false
ingress: true
ingress_port: 8100
panel_icon: mdi:heart-pulse
panel_title: Core
homeassistant_api: true
hassio_api: true
hassio_role: manager
map:
- addon_config:rw
- config:rw
- media:rw
- share:rw
options:
  db_host: 77b2833f-timescaledb
  db_port: 5432
  db_name: paddicore
  db_user: postgres
  db_password: homeassistant
  postgres_superuser_password: ''
  postgres_superuser_password_old: ''
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  postgres_superuser_password: password?
  postgres_superuser_password_old: password?
image: ghcr.io/paddisense/{arch}-paddisense-core
