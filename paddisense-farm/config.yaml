# GENERATED from the source config.yaml by release.sh Step 7 (WR-PS-115).
# DO NOT hand-edit — any edit is overwritten at the next release.
# source: PaddiSense/Farm@1a31e54683229f1aece664366dd4e7f5284d4216
# Catalog-only deltas are declared in contracts/catalog-overrides.yaml;
# contracts/check-catalog-drift.py verifies catalog == source on every release + in CI.
name: PaddiSense Farm
version: 2026.8.24
slug: paddisense-farm
description: PaddiSense Farm — paddock, crop and event management for rice farming
logo: true
icon: true
url: https://github.com/PaddiSense/Farm
arch:
- amd64
- aarch64
startup: application
init: false
ingress: true
ingress_port: 8106
panel_icon: mdi:tractor
panel_title: Farm
homeassistant_api: true
hassio_api: true
hassio_role: manager
map:
- config:rw
- addon_config:rw
- media:rw
- share:rw
options:
  db_host: 77b2833f-timescaledb
  db_port: 5432
  db_name: paddisense_farm
  db_user: farm_owner
  db_password: ''
  seed_author_mode: false
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  seed_author_mode: bool?
image: ghcr.io/paddisense/{arch}-paddisense-farm
