# paddisense-planner (grower catalog entry)

Created 2026-08-03 for Planner's FIRST grower release (WR-PS-203). `config.yaml` here is a
GENERATED artifact — `release.sh` regenerates it from the source addon's config.yaml on every
release (WR-PS-115) and refuses to push if it drifts from source. Do not hand-edit it.

This README exists because the catalog generator writes into this directory but does not
create it, and git does not track empty directories.
