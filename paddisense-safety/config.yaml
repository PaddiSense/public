# GENERATED from the source config.yaml by release.sh Step 7 (WR-PS-115).
# DO NOT hand-edit — any edit is overwritten at the next release.
# source: PaddiSense/Safety@a2daadb24049040f3d29252ba40b557029d9d7d6
# Catalog-only deltas are declared in contracts/catalog-overrides.yaml;
# contracts/check-catalog-drift.py verifies catalog == source on every release + in CI.
name: PaddiSense Safety
version: 2026.8.9
slug: paddisense-safety
description: PaddiSense Safety — Worker Safety System (WSS) with zone monitoring and
  escalation alerts
logo: true
icon: true
url: https://github.com/PaddiSense/Safety
arch:
- amd64
- aarch64
startup: application
init: false
ingress: true
ingress_port: 8097
ports:
  8097/tcp: null
ports_description:
  8097/tcp: Office-TV kiosk display (optional). Map to a free host port + set kiosk_key
    to enable; /kiosk is token-gated, the rest stays behind login. LAN only.
panel_icon: mdi:shield-account
panel_title: Safety
homeassistant_api: true
hassio_api: true
hassio_role: manager
map:
- addon_config:rw
- media:rw
- share:rw
options:
  db_host: 77b2833f-timescaledb
  db_port: 5432
  db_name: paddisense_safety
  db_user: postgres
  db_password: homeassistant
  admin_key: ''
  kiosk_key: ''
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  admin_key: password?
  kiosk_key: password?
image: ghcr.io/paddisense/{arch}-paddisense-safety
