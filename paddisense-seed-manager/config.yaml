name: PaddiSense Seed Manager
version: "2026.6.170"
slug: paddisense-seed-manager
description: PaddiSense Seed Manager — seed inventory, movements, grading, and moisture tracking
logo: true
icon: true
url: https://github.com/PaddiSense/SeedManager
image: ghcr.io/paddisense/{arch}-paddisense-seed-manager
arch:
  - amd64
  - aarch64
startup: application
ingress: true
ingress_port: 8098
homeassistant_api: true
panel_icon: mdi:seed
panel_title: Seed Manager
map:
  - addon_config:rw
  - config:rw
  - media:rw
  - share:rw
options:
  db_host: "77b2833f-timescaledb"
  db_port: 5432
  db_name: "seedmgr"
  db_user: "postgres"
  db_password: "homeassistant"
  plc_host: ""
  plc_port: 502
  admin_key: ""
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  plc_host: str?
  plc_port: port?
  admin_key: str?
