# GENERATED from the source config.yaml by release.sh Step 7 (WR-PS-115).
# DO NOT hand-edit — any edit is overwritten at the next release.
# source: PaddiSense/Weather@a02d2ffd80708268b5cea9cee77d95ebb4bdf06d
# Catalog-only deltas are declared in contracts/catalog-overrides.yaml;
# contracts/check-catalog-drift.py verifies catalog == source on every release + in CI.
name: PaddiSense Weather
version: 2026.8.17
slug: paddisense-weather
description: PaddiSense Weather — weather stations, forecast, burn forecast, spray
  assessment
logo: true
icon: true
url: https://github.com/PaddiSense/Weather
arch:
- amd64
- aarch64
startup: application
init: false
ingress: true
ingress_port: 8105
panel_icon: mdi:weather-partly-cloudy
panel_title: Weather
homeassistant_api: true
hassio_api: true
map:
- addon_config:rw
- media:rw
- share:rw
options:
  db_host: 77b2833f-timescaledb
  db_port: 5432
  db_name: paddisense_weather
  db_user: weather_owner
  db_password: ''
  admin_key: ''
schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  admin_key: password?
image: ghcr.io/paddisense/{arch}-paddisense-weather
